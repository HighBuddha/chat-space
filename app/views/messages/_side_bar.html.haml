.side-bar
  %Header
    .top-items
      .top-items__name 
        = current_user.name
      .top-items__icons 
        = link_to new_group_path, class: "Contents__btn Contents__btn--edit" do
          =icon('far', 'edit')
        = link_to edit_user_path(current_user), class: "Contents__btn Contents__btn--system" do
          =icon('fas', 'cog')
  .side-bar__list
    - current_user.groups.each do |group|
      .side-bar__group-list
        = link_to group_messages_path(group) do
          .side-bar__group-name 
            = group.name
          .side-bar__message メッセージはまだありません

